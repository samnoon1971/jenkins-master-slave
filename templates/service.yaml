apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkins-msarch.fullname" . }}
  labels:
    {{- include "jenkins-msarch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jenkins-msarch.selectorLabels" . | nindent 4 }}
